#!/bin/bash
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
cd $SCRIPT_DIR/..
echo "Creating Migrations for Version $1 in Container"
$SCRIPT_DIR/runinpypgstac --mountmigrations --localuser stageversion $@
